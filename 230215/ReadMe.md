```sh
yarn create react-app board << directory부터 만들어주는 것
yarn create react-app  ./ << directory안에서 실행
yarn add express express-session morgan cookie-parser dotenv
yarn add styled-components react-router-dom react react-redux
yarn add mysql2 sequelize sequelize-cli axios
server 폴더 만들고 그 안에 npx sequelize init
yarn add -D nodemon
yarn init
```
